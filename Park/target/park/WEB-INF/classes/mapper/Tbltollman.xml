<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.park.mapper.TollmanMapper">
	<select id="tollmanLogin"  resultType="com.park.entity.TblBackUser">
		select * from tbbackuser where userid = #{userid} and upass = #{upass}  and roleid= 10
	</select>
<!--	<update id="delBackUser" parameterType="java.lang.String">-->
<!--		update tbbackuser set isdeleted = 1 where userid = #{userid};-->
<!--	</update>-->
</mapper>
	